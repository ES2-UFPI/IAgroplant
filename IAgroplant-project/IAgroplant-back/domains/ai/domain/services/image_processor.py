class ImageProcessor:

    @staticmethod
    def process(
        image: bytes,
    ) -> bytes:

        """
        Futuramente:

        • reduzir resolução
        • remover EXIF
        • converter JPEG
        • comprimir

        """

        return image